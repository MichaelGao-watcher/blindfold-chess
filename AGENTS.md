# AGENTS.md — Blindfold Chess（盲棋项目）

> 本文件是 **AI 规则手册**，定义触发词、文件职责、恢复流程和行为约束。  
> **如无架构变更，请勿修改本文件。**  
> 动态进度和状态请查看同目录下的 `STATE.md`。

---

## 项目概述

- **名称**：blindfold-chess（盲棋）
- **类型**：可互动的网页 / HTML 应用
- **技术栈**：纯 HTML + CSS + JS（手写，不引入框架）
- **仓库**：https://github.com/MichaelGao-watcher/blindfold-chess
- **本地路径**：`E:\工作文件\blindfold-chess`

---

## 文件职责矩阵

> **每个文件只干一件事，不越界。**

| 文件 | 核心职责 | 面向谁 | 何时修改 |
|------|---------|--------|---------|
| `AGENTS.md` | **规则手册**：触发词定义、文件职责、AI 行为约束、恢复流程 | AI | 极少（架构变更时） |
| `STATE.md` | **动态状态**：当前任务、开发进度、待办、会话摘要、已知问题 | AI | 每次会话结束（通过"存档"口令） |
| `README.md` | **项目门户**：项目介绍、运行方式、技术决策记录 | 人类 / 外部 | 偶尔 |
| `备忘录.md` | **速查表**：Git 命令、终端操作、技术概念解释 | 人类 | 很少 |
| `index.html` | **实际产品**：正在开发的盲棋页面 | 用户 | 持续 |

---

## 技术决策（已确定，勿改）

| 决策项 | 选择 | 原因 |
|--------|------|------|
| 前端技术栈 | **HTML + CSS + JS** | 项目规模小，纯手写完全够用 |
| 框架 | **暂不使用 React / Vue** | 当前只需一个互动棋盘页面，框架会增加不必要复杂度 |
| 后续扩展 | 如需在线对战、用户系统等功能，再评估引入框架 |

> 记录日期：2026-05-15

---

## 开发方式

**无外部依赖**，无需 npm / pip / 任何包管理器：
- 直接用 VS Code 编辑 `index.html`
- 双击 `index.html` 用浏览器打开即可预览
- 改完保存（Ctrl+S），浏览器按 F5 刷新看效果

---

## 触发词手册

> 用户说出以下关键词时，AI 执行对应动作。

### 恢复类
| 触发词 | 动作 |
|--------|------|
| "恢复" / "继续" / "状态" / "检查过状态" | **恢复项目上下文**：先读 `AGENTS.md`（了解规则），再读 `STATE.md`（了解动态进度），最后汇报给用户 |

### 存档类
| 触发词 | 动作 |
|--------|------|
| "存档" / "保存状态" / "结束会话" / "收工" / "今天先到这" | **保存当前进度**：读取当前 `STATE.md` → 更新"当前会话摘要"、"开发进度"、"待办列表"、"最近决策/变更" → 保存 `STATE.md` → `git add .` → `git commit -m "存档：xxx（日期）"` → `git push` |

### 辅助类
| 触发词 | 动作 |
|--------|------|
| Git 相关问题 | 引导用户查看 `备忘录.md` |
| 技术概念问题 | 引导用户查看 `备忘录.md` |

---

## 给后续 AI 的提醒

- 本项目**没有敏感配置**（无 .env、无 API 密钥）
- **不要擅自引入框架**（React/Vue/npm 等），除非用户明确要求
- 所有修改应最小化，保持代码简洁易懂
- 修改后记得 `git add` → `git commit` → `git push`
- **不要猜测用户意图**——盲棋的具体功能设计（棋盘样式、交互方式、规则等）必须由用户明确说明后再实现
