# Task: SettingsModule（设置模块）

> 模块状态：已实现（部分）  
> 目标：提供主题、语言、音效、引擎参数等设置，并持久化保存  
> 设计文档：high-Level Design.md §4.6

---

## 子任务清单

### 设置面板界面
- [ ] **S-01** 创建设置面板 HTML 结构（弹窗或独立页面）
- [ ] **S-02** 调用 `SettingsModule.init()` 初始化设置面板
- [ ] **S-03** 设置项分组布局：外观 / 语言 / 音效 / 引擎 / 辅助功能

### 主题切换
- [ ] **S-04** 深色/浅色主题切换开关（或单选按钮）
- [ ] **S-05** `SettingsModule.setTheme('dark'|'light')` 切换主题
- [ ] **S-06** 主题切换后全站 UI 实时同步更新
- [ ] **S-07** CSS 变量定义两套主题色值，通过 `data-theme` 属性切换

### 语言切换
- [ ] **S-08** 中文/英文语言切换开关
- [ ] **S-09** `SettingsModule.setLanguage('zh'|'en')` 切换语言
- [ ] **S-10** 语言切换后全站文本实时更新（使用统一语言字典）
- [ ] **S-11** 语言字典结构：`lang/zh.js` 和 `lang/en.js`

### 音效设置
- [ ] **S-12** 音效开关（走子音效 开/关）
- [ ] **S-13** `SettingsModule.setSound(enabled)` 控制音效
- [ ] **S-14** 音效文件准备（走子声：短促木质或合成音）

### 引擎难度自定义
- [ ] **S-15** 引擎难度设置界面：预设按钮 + 自定义滑动条
- [ ] **S-16** 滑动条范围：Elo 400 ~ 3200，步进 50 或 100
- [ ] **S-17** 滑动条实时显示当前 Elo 数值
- [ ] **S-18** `SettingsModule.setEngineConfig({ elo })` 保存引擎配置
- [ ] **S-19** 自定义 Elo 映射到 Stockfish Skill Level / Depth 参数

### 候选走法辅助开关
- [ ] **S-20** "显示引擎建议"开关（默认关闭）
- [ ] **S-21** 开关状态影响 BlindfoldModule 是否显示 Top 3 候选走法
- [ ] **S-22** 开关保存到 StorageModule

### 设置持久化
- [ ] **S-23** 所有设置项自动保存到 `StorageModule`（localStorage）
- [ ] **S-24** 页面刷新后自动读取并应用已保存的设置
- [ ] **S-25** `SettingsModule.get(key)` 获取某项设置值
- [ ] **S-26** 设置默认值：深色主题、中文、音效开、Easy、建议关

### 测试与优化
- [ ] **S-27** 测试主题切换后全站 UI 同步更新
- [ ] **S-28** 测试语言切换后全站文本同步更新
- [ ] **S-29** 测试设置项刷新后是否保留
- [ ] **S-30** 测试滑动条 Elo 值是否正确映射到引擎参数
